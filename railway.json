{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r Salesphere/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd Salesphere && python manage.py migrate && gunicorn Salesphere.wsgi"
  }
}